// cross-q-context — the composed EXECUTION contract (ADR-213 Layer 2). The public `execute(input)`
// surface: it keys off both the primitives (`contract.ts`) and the data model (`model.ts`), so it
// lives one layer above them.
export {};
